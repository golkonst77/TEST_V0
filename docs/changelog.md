# Changelog

## [2025-01-15] - Создание системы управления секциями главной страницы

### Добавлено
- **Система управления секциями главной страницы** в админке (`/admin/content`)
- **API endpoint** `/api/admin/content` для управления секциями
- **Хук `useHomepageSections`** для загрузки конфигурации секций
- **Файл конфигурации** `data/homepage-sections.json` для хранения статусов секций
- **Компонент `Technologies`** для отображения используемых технологий
- **Функциональность включения/отключения секций** на главной странице
- **Управление порядком секций** с помощью стрелок вверх/вниз
- **Предпросмотр секций** с переходом на главную страницу

### Изменено
- **Переработана страница админки** `/admin/content` - теперь управляет секциями вместо страниц
- **Обновлена главная страница** - теперь использует конфигурацию секций для отображения
- **Добавлены переключатели** для каждой секции с мгновенным применением изменений
- **Улучшен интерфейс** с иконками, описаниями и статистикой

### Технические детали
- Создан API для управления секциями с сохранением в JSON файл
- Реализован хук для загрузки конфигурации с fallback значениями
- Добавлена поддержка 11 секций: Hero, About, Services, Calculator, Pricing, Reviews, Guarantees, FAQ, News, Contacts, Technologies
- Конфигурация сохраняется в `data/homepage-sections.json`
- Создан отдельный API endpoint `/api/homepage-sections` для получения актуальной конфигурации
- Изменения применяются мгновенно после обновления главной страницы

## [2025-01-15] - Исправление настроек по умолчанию и синхронизация с базой данных

### Исправлено
- **Исправлены настройки по умолчанию в lib/settings-store.ts** - теперь соответствуют данным из Supabase:
  - Email: `urist40@gmail.com` (вместо `info@prostoburo.ru`)
  - Адрес: `Калуга, Дзержинского 37, офис 20` (вместо `г. Калуга`)
  - Телефон: `+7953 777 77 77` (вместо `+7 953 330-17-77`)
  - Название: `Просто Бюро` (вместо `ПростоБюро`)
  - Описание: `Бухгалтерские услуги` (вместо `Профессиональная бухгалтерская компания`)
  - **Суббота: `По согласованию`** (вместо `Выходной`)
- **Исправлены fallback значения в компонентах**:
  - `components/contacts.tsx` - убран fallback на `golkonst@gmail.com`, обновлен fallback для субботы
  - `components/reviews.tsx` - обновлены fallback значения адреса и телефона
- **Подтверждена корректная работа с Supabase** - данные загружаются из базы данных, а не из локального fallback
- **Устранена несогласованность данных** между базой данных и отображением на сайте

### Технические детали
- Обновлены локальные настройки по умолчанию в `lib/settings-store.ts`
- Исправлены fallback значения в функции `mapDatabaseToSettings()`
- Убраны захардкоженные значения в компонентах
- Данные теперь корректно синхронизированы между Supabase и фронтендом
- Создан SQL-скрипт `update_saturday_hours.sql` для обновления данных в базе

## [2025-01-15] - Исправление загрузки файлов и упрощение доступа к админке

### Изменено
- Упрощен скрытый доступ к админке: теперь сразу показывается форма входа с полями логина и пароля
- Убрано промежуточное окно с кнопкой "Войти в админку"
- Добавлена обработка ошибок входа и индикатор загрузки
- Улучшен UX: форма отправляется по Enter, добавлены плейсхолдеры
- Исправлена проблема с неработающей кнопкой входа
- **После успешного входа пользователь сразу перенаправляется в админку с левым меню**
- Убрана логика проверки аутентификации и промежуточное меню
- **Переведена загрузка файлов с Supabase Storage на локальное сохранение в папку uploads**

### Исправлено
- Исправлена ошибка аутентификации: поле `login` заменено на `username` для соответствия API
- Улучшена обработка ответов от API аутентификации
- **Исправлена ошибка загрузки фоновых изображений в админке**
- Файлы теперь сохраняются локально в `/public/uploads/` вместо Supabase Storage
- **Исправлено выравнивание заголовков в компонентах Services, Guarantees, Contacts, PricingSection, FAQ, Calculator и News - теперь по центру**
- **Увеличен размер текста "Расти спокойно" в баннере для лучшей заметности**

## [2025-01-15] - Исправление выравнивания текста в компонентах

### Исправлено
- Исправлено выравнивание заголовка "Новости и статьи" в компоненте news.tsx - изменено с `text-center md:text-left` на `text-center`
- Исправлено выравнивание описания в компоненте news.tsx - изменено с `text-center md:text-left` на `text-center`
- Удалено примечание в компоненте pricing-section.tsx для улучшения визуального восприятия
- Увеличен размер текста "Расти спокойно" в компоненте guarantees.tsx с `text-2xl md:text-3xl lg:text-4xl font-semibold` на `text-3xl md:text-4xl lg:text-5xl xl:text-6xl font-bold`
- Исправлено выравнивание иконок в компоненте contacts.tsx - изменено с `justify-start` на `justify-center`
- Исправлено выравнивание иконок в компоненте services.tsx - изменено с `justify-start` на `justify-center`
- Исправлен рейтинг в компоненте reviews.tsx с 4.8 на 5.0
- Исправлен адрес в компоненте reviews.tsx с "г. Калуга, ул. Примерная, 123" на "г. Калуга"

### Технические детали
- Все заголовки секций теперь выровнены по центру для лучшей визуальной консистентности
- Все иконки в карточках теперь центрированы для лучшего визуального восприятия
- Улучшена читаемость и визуальная иерархия текста
- Сохранена адаптивность для мобильных устройств
- Исправлены захардкоженные данные в компоненте отзывов

## [2025-01-15] - Динамическое получение рейтинга и адреса в компоненте отзывов

### Добавлено
- Динамическое получение рейтинга с Яндекс.Карт через API `/api/yandex-reviews`
- Динамическое получение адреса из настроек админки через API `/api/admin/settings`
- Интерфейсы для типизации данных: `CompanyInfo`, `Settings`
- Функции `fetchSettings()` и `fetchCompanyInfo()` для получения данных
- Отображение реального количества отзывов из агрегатора

### Изменено
- Компонент `reviews.tsx` теперь получает данные динамически вместо захардкоженных значений
- Рейтинг отображается как `{companyInfo.rating?.toFixed(1) || '5.0'}`
- Адрес отображается как `{settings.address}` вместо захардкоженного "г. Калуга, ул. Примерная, 123"
- Телефон отображается как `{settings.phone}` из настроек админки
- Количество отзывов отображается как `{totalReviews}` из агрегатора

### Технические детали
- Добавлены состояния `companyInfo` и `settings` для хранения данных
- Реализована обработка ошибок с fallback значениями
- Компонент теперь полностью динамический и синхронизирован с реальными данными
- Улучшена типизация TypeScript с новыми интерфейсами

## [2025-01-07] - Создание раздела "Контактная информация" и реорганизация настроек

### Добавлено
- Создан новый раздел "Контактная информация" в админке (`/admin/contacts`)
- Добавлен пункт "Контактная информация" в навигацию раздела "Контент сайта"
- Реализована синхронизация контактных данных между разделами header и contacts
- Создана страница для управления всеми контактами компании в одном месте

### Изменено
- Обновлена навигация админки - добавлен пункт "Контактная информация" в "Контент сайта"
- Реорганизована страница "Настройки" - убрана контактная информация
- Упрощена структура настроек - оставлены только системные настройки и основная информация
- Оптимизирована логика сохранения контактных данных
- Изменен адрес по умолчанию с "г. Москва" на "г. Калуга" во всех источниках данных

### Исправлено
- Устранено дублирование контактной информации между разделами "Настройки" и "Шапка сайта"
- Решена проблема несогласованности данных в разных разделах админки
- Улучшена структура админки для более логичного управления контентом
- Исправлена проблема с отображением адреса на фронтенде после изменения в админке

### Удалено
- Удалена отладочная кнопка "Войти в админку" с главной страницы
- Убрано сообщение с тестовыми данными для входа на странице авторизации
- Удален компонент FloatingAdminButton

### Добавлено
- Создан скрытый способ доступа к админке через правый нижний угол сайта
- Добавлен компонент HiddenAdminAccess с индикатором прогресса
- Реализован диалог быстрого доступа к разделам админки

## [2025-01-07] - Реорганизация админки и интеграция с Supabase

### Добавлено
- Создан SQL скрипт `create-header-config-table.sql` для создания таблицы `header_config` в Supabase
- Добавлена интеграция API header с Supabase для сохранения конфигурации в базе данных
- Реализован глубокий merge (deepMerge) для корректного обновления вложенных полей конфигурации
- Добавлен fallback на файловое хранилище при недоступности Supabase
- **Реорганизована структура админки** - объединены похожие разделы в логические группы

### Изменено
- Обновлён `app/api/admin/header/route.ts` для работы с Supabase вместо только файлов JSON
- Улучшена обработка ошибок с автоматическим fallback на файловое хранилище
- Оптимизирована структура дефолтной конфигурации header
- **Полностью переработана навигация админки** (`components/admin-layout.tsx`):
  - **Контент сайта**: Главная страница, Шапка сайта, Контент, Медиафайлы
  - **Коммерция**: Калькулятор, Тарифы, Купоны
  - **Интерактив**: Чек-листы, Отзывы, Видеоотзывы
  - **Маркетинг**: Подписчики, Кампании
  - **Управление**: Пользователи, Настройки
- Сокращено количество пунктов меню с 14 до 7 основных разделов

### Исправлено
- Решена проблема с несохранением поля `logo.text` (названия сайта) - теперь используется глубокий merge
- Исправлена потеря данных при частичном обновлении конфигурации header
- Устранены ошибки компиляции в admin-layout.tsx
- Исправлена структура навигации с правильными иконками и типами

### Удалено
- Удалена старая структура menuItems в admin-layout.tsx
- Убраны дублированные функции и интерфейсы

## [2025-01-07] - Интеграция Header API с Supabase

### Добавлено
- Создан SQL скрипт `create-header-config-table.sql` для создания таблицы `header_config` в Supabase
- Добавлена интеграция API header с Supabase для сохранения конфигурации в базе данных
- Реализован глубокий merge (deepMerge) для корректного обновления вложенных полей конфигурации
- Добавлен fallback на файловое хранилище при недоступности Supabase

### Изменено
- Обновлён `app/api/admin/header/route.ts` для работы с Supabase вместо только файлов JSON
- Улучшена обработка ошибок с автоматическим fallback на файловое хранилище
- Оптимизирована структура дефолтной конфигурации header

### Исправлено
- Решена проблема с несохранением поля `logo.text` (названия сайта) - теперь используется глубокий merge
- Исправлена потеря данных при частичном обновлении конфигурации header

## [2025-01-07] - Исправление режима технического обслуживания

### Добавлено
- Создан компонент `MaintenancePage` для отображения страницы технического обслуживания
- Добавлен API endpoint `/api/maintenance` для проверки статуса maintenance mode
- Реализован компонент `MaintenanceWrapper` для оборачивания приложения
- Интегрирована проверка maintenance mode в layout приложения
- Добавлено исключение для админки - maintenance mode не применяется к `/admin/*`

### Изменено
- Обновлён `app/layout.tsx` для интеграции с MaintenanceWrapper
- Улучшена обработка ошибок в API settings с подробным логированием

### Исправлено
- Решена проблема с неработающим режимом технического обслуживания
- Исправлена валидация обязательных полей в API settings
- Добавлены дефолтные значения для предотвращения ошибок

## [2025-01-07] - Настройка переменных окружения

### Добавлено
- Создан файл `.env.local` с переменными окружения Supabase
- Добавлены переменные: `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### Исправлено
- Исправлена ошибка "Не удалось сохранить настройки" в админке
- Решена проблема с отсутствием подключения к Supabase
- Настройки теперь сохраняются в базе данных, а не только в памяти процесса

## [2025-01-07] - Обновление зависимостей и структуры проекта

### Добавлено
- Обновлены зависимости: Next.js 15.0.4, React 18.3.1, TypeScript 5.4.2
- Добавлены новые зависимости: @supabase/supabase-js, zod, @hookform/resolvers
- Создана структура документации в папке `/docs`

### Изменено
- Обновлены скрипты в package.json для совместимости с новыми версиями
- Улучшена структура проекта с разделением на логические модули
- Оптимизированы настройки TypeScript и ESLint

### Исправлено
- Исправлены синтаксические ошибки с операторами ?? и ||
- Устранены предупреждения о неиспользуемых переменных
- Исправлены ошибки типизации в компонентах 