# AUSN Blob Button (розовая кнопка)

## Что это
Розовая фиксированная «blob»‑кнопка на основном сайте, ведущая на под‑сайт АУСН.

## Где лежит
- Компонент: `components/AusnBlobButton.tsx`
- Подключение глобально: `app/layout.tsx` (рендерится сразу после `<Header />`)
- Стили формы/анимации: `app/globals.css` (классы `.blob-ausn` и `.blob-ausn-vibrate`)

## Как поменять
- **Текст на кнопке**: правь JSX внутри `AusnBlobButton.tsx` (сейчас: `АУСН`)
- **Ссылка**: правь `href` в `AusnBlobButton.tsx` (сейчас: `/ausn/`)
- **Размер/позиция**: Tailwind‑классы в `className` компонента

## Примечания
- Кнопка сделана фиксированной и всегда поверх контента (через `z-[60]`).
- На мобильных позиционируется снизу‑справа, на desktop — сверху‑справа.
